# narwhal-sql

Dialect-aware SQL statement splitting and formatting.

Statement boundary detection (the only piece of SQL parsing in the workspace) plus dialect-specific reformatters. Used by completion, dump and the editor.

Part of the [narwhal](../../README.md) workspace. See [`docs/ARCHITECTURE.md`](../../docs/ARCHITECTURE.md) for the layered crate map.
